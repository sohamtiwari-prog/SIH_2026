numpy>=1.24
scipy>=1.10
opencv-contrib-python>=4.8
scikit-image>=0.21
pandas>=2.0

# optional: georeferenced/GeoTIFF reference product support (io.py falls back
# to a built-in PDS3 reader and a minimal TIFF reader when this is absent)
rasterio>=1.3

# optional: used only by lunareg.stats for the failure-prediction study
scikit-learn>=1.3

# dev / test
pytest>=7.4
pytest-cov>=4.1
